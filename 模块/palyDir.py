#!/usr/bin/python3.7
# -*- coding:UTF-8 -*-

# dir函数

## 作用
 # 内置的dir() 函数可以返回对象所定义的名称列表
 # 如果这一对象是一个模块，
 # 就会返回这个一个对象的所有的函数，变量，类

## 而且，dir接受参数，如果参数是一个模块的名称，
 # 函数会返回指定模块的名称列表，
 # 如果没有提供参数，函数会返回当前模块的名称列表

dir()

